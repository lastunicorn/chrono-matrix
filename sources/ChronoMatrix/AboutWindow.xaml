<Window
    x:Class="DustInTheWind.ChronoMatrix.AboutWindow"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:DustInTheWind.ChronoMatrix"
    
    mc:Ignorable="d"
    Title="About Chrono Matrix"
    Height="300"
    Width="400"
    d:DataContext="{d:DesignInstance local:AboutViewModel}"
    ResizeMode="NoResize"
    WindowStyle="SingleBorderWindow"
    WindowStartupLocation="CenterOwner"
    Background="#1E1E1E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Text="Chrono Matrix"
            FontSize="24"
            FontWeight="Bold"
            Foreground="White"
            HorizontalAlignment="Center"
            Margin="0 0 0 10"/>

        <TextBlock
            Grid.Row="1"
            FontSize="14"
            Foreground="Gray"
            HorizontalAlignment="Center"
            Margin="0 0 0 20">
            
            <Run Text="Version" />
            <Run Text="{Binding Version, Mode=OneWay}" />
            
        </TextBlock>

        <TextBlock
            Grid.Row="2"
            Text="A digital clock displayed using the hands of 96 analog clocks."
            FontSize="12"
            Foreground="LightGray"
            TextWrapping="Wrap"
            HorizontalAlignment="Center"
            TextAlignment="Center"
            Margin="0 0 0 20"/>

        <StackPanel
            Grid.Row="3"
            Orientation="Vertical"
            HorizontalAlignment="Center"
            Margin="0 0 0 20">

            <TextBlock
                FontSize="12"
                Foreground="LightGray"
                Margin="0 0 0 5">
                
                <Run Text="Copyright &#169; " />
                <Run Text="{Binding CopyrightYear, Mode=OneWay}" />
                <Run Text=" Dust in the Wind" />
                
            </TextBlock>

            <TextBlock
                FontSize="12"
                Foreground="LightGray"
                TextAlignment="Center">
                
                <Hyperlink
                    NavigateUri="https://github.com/lastunicorn/chrono-matrix"
                    RequestNavigate="Hyperlink_RequestNavigate"
                    Foreground="DodgerBlue">
                    https://github.com/lastunicorn/chrono-matrix
                </Hyperlink>
                
            </TextBlock>

        </StackPanel>

        <Button
            Grid.Row="5"
            Content="Close"
            Width="80"
            Height="30"
            HorizontalAlignment="Center"
            Click="CloseButton_Click"
            IsCancel="True">

            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Background" Value="#3E3E3E"/>
                    <Setter Property="BorderBrush" Value="Gray"/>
                    <Setter Property="BorderThickness" Value="1"/>

                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#4E4E4E"/>
                        </Trigger>
                    </Style.Triggers>

                </Style>
            </Button.Style>

        </Button>

    </Grid>
</Window>
