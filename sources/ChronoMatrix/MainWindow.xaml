<Window
    x:Class="DustInTheWind.ChronoMatrix.MainWindow"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:clockwpf="clr-namespace:DustInTheWind.ClockWpf;assembly=DustInTheWind.ClockWpf"
    xmlns:wpfToolkit="clr-namespace:DustInTheWind.WpfToolkit;assembly=DustInTheWind.WpfToolkit"
    xmlns:clocks="clr-namespace:DustInTheWind.ChronoMatrix.Controls;assembly=DustInTheWind.ChronoMatrix.Controls"
    xmlns:local="clr-namespace:DustInTheWind.ChronoMatrix"
    
    mc:Ignorable="d"
    Title="Chrono Matrix"
    Height="450"
    Width="800"
    d:DataContext="{d:DesignInstance local:MainViewModel}"
    ResizeMode="CanResizeWithGrip"
    wpfToolkit:WindowDragBehavior.EnableDrag="True"
    WindowStyle="None"
    WindowStartupLocation="CenterScreen">

    <local:OverlayPanel>

        <clocks:MatrixClock46
            Margin="20 30"
            BlinkingColon="{Binding AreColonsBlinking, Mode=OneWay}"
            ShowSeconds="{Binding ShowSeconds, Mode=OneWay}" />

        <wpfToolkit:ShyControl
            HorizontalAlignment="Left"
            VerticalAlignment="Bottom">

            <StackPanel
                Orientation="Horizontal">

                <Button
                    Width="24"
                    Height="24"
                    Content="?"
                    Margin="0 0 10 0"
                    Command="{Binding AboutCommand}"/>

                <TextBlock
                    FontSize="12"
                    Foreground="Gray"
                    VerticalAlignment="Center">
                    
                    <Run Text="Chrono Matrix" />
                    <Run Text="{Binding Version, Mode=OneWay}" />

                </TextBlock>

            </StackPanel>

        </wpfToolkit:ShyControl>

        <wpfToolkit:ShyControl
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Padding="0">

            <Button
                Content="✕"
                FontSize="16"
                Padding="10,5"
                Cursor="Hand"
                Command="{Binding ExitAppCommand}"
                Style="{StaticResource CloseButton}"/>

        </wpfToolkit:ShyControl>

        <wpfToolkit:ShyControl
            HorizontalAlignment="Left"
            VerticalAlignment="Top">

            <StackPanel
                Orientation="Horizontal">

                <CheckBox
                    Content="Blinking Colons"
                    IsChecked="{Binding AreColonsBlinking}" />

                <CheckBox
                    Content="Show Seconds"
                    IsChecked="{Binding ShowSeconds}"
                    Margin="10 0 0 0"/>

            </StackPanel>

        </wpfToolkit:ShyControl>
        
    </local:OverlayPanel>
</Window>
